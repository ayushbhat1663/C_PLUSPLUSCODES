class Solution {
  public:
    void segregate0and1(vector<int> &a) {
        // code here
        int pos = -1, n = a.size();
        for(int i = 0; i < n; i++){
            if(a[i] == 0)
                swap(a[i], a[++pos]);
        }
    }
};
